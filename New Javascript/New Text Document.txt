
today i learn string indexing , .length ,trim(), ToupperCase,toLowercase,slice(), Type of opreators,
convert number to string,string to number, string concatination, template String ,
booleans and comparison Opreators ,if Else ,nested if else ,if else else if else , switch statement and while loop,
 for loop, break keyword, Continue Keyword,Araay, push ,pop,unshift ,shift ,clone array,for loop in array ,array destructring, objects,objects itrartion, object destructringobjects inside array i just learn these and i jsut start javascript

 please can you give me a easy task about these




Trim any leading or trailing whitespace from the string.
Check if the length of the string is greater than 5. If it is, slice the string to only include the first 5 characters.
Convert the sliced string to uppercase.
Concatenate the sliced and uppercase string with the string " is awesome!" using template strings.
Check if the resulting string starts with the letter 'J'. If it does, return the string itself; otherwise, return "Invalid"
